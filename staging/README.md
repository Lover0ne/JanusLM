# Staging

This folder is used internally by the ingestion process.

- Documents in transit between pipeline steps are temporarily placed here
- Content is moved automatically and should not be edited manually
- The folder can be safely emptied if no ingestion is in progress
