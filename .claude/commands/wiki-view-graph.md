Open the interactive knowledge graph visualization in the browser.

Usage: /wiki-view-graph

Run `python tools/print_graph.py --open` to render graph.json as HTML and open it.
If graph.json does not exist, run `python tools/build_graph.py` first.
