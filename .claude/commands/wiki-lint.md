Run a semantic quality analysis on the wiki content.

Usage: /wiki-lint

Checks for orphan pages, broken links, contradictions, stale summaries,
missing entity pages, mixed content, and data gaps.

Load the `/maintainer` skill and follow the Lint Workflow defined there.
