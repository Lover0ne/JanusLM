Check or toggle wiki permission flags (can_modify, can_forget, can_anonymize_pii).

Usage: /wiki-protect

Run `python tools/wiki_protect.py --status` to show current flags.
To toggle a flag: `python tools/wiki_protect.py --toggle <flag_name>`.
