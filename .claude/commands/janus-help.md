Show JanusLM capabilities, tools, and usage examples.

Usage: /janus-help

Run `python tools/help.py` and present the output to the user.
Use `--detail` for full examples, `--area <name>` to filter by area.
