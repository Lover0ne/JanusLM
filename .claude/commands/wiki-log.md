Show recent wiki activity from the operation log.

Usage: /wiki-log

Run `python tools/log_report.py --summary` for a human-readable report.
Use `--type <op>` to filter (ingest, heal, graph), `--last N` for recent days,
or `--json` for structured output.
