Verify and regenerate the project directory structure.

Usage: /scaffold

Run `python tools/scaffold.py --check` to verify, or `python tools/scaffold.py --fix`
to recreate any missing directories or scaffold files.
Idempotent — safe to run at any time.
